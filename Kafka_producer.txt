 Kafka-Producer App integration 
 =======================
1. application.yml => Kafka Releated property needs to update 

Exmaple
====
kafka:
  producer:
    enabled: true
    bootstrap-servers: pkc-xxxxx.us-east-1.aws.confluent.cloud:9092
    user: YOUR_CONFLUENT_API_KEY
    password: YOUR_CONFLUENT_API_SECRET
    acks: all
    retries: 3
    additional-properties:
      max.block.ms: "5000"
      request.timeout.ms: "30000"

1. application-unit-test.yml => Kafka Releated property needs to update 

Exmaple
====
kafka:
  producer:
    enabled: false
    bootstrap-servers: pkc-xxxxx.us-east-1.aws.confluent.cloud:9092
    user: YOUR_CONFLUENT_API_KEY
    password: YOUR_CONFLUENT_API_SECRET
    acks: all
    retries: 3
    additional-properties:
      max.block.ms: "5000"
      request.timeout.ms: "30000"
	  
2. pom.xml below dependency needs to add


        <dependency>
			<groupId>com.bbw.kafka</groupId>
			<artifactId>kafka-producer</artifactId>
			<version>1.0.0</version>
		</dependency>
4. Mock KafkaProducerProperties 
3. Test class should exclude the KafkaProducerProperties.class , kafkaProducerFactory & KafkaTemplate